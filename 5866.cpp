#include <iostream>
#include <queue>
using namespace std;
int main()
{
	cin.tie(0);
	std::ios_base::sync_with_stdio(0);

	int b, e;
	cin >> b >> e;
	vector<int> xb(b), xe(e);
	for (auto& i : xb) cin >> i;
	for (auto& i : xe) cin >> i;

	int anx = 0;
	
	int bdx = 0, edx = 0;
	pair<char, int> bg, eg;
	for (int i=0; i<)
}